 r←MS3_init_HR dummy
⍝ vars are global by design here ;)
⍝ nneds the following repos as siblings: GhostRider,
 :If ''≡r←MS3_init_common'-cef --remote-debugging-port=8080'
     config←'HTMLRenderer'  ⍝ name the config we want to run against
 :EndIf
